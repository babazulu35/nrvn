/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EventDatesByPerformancesPipe } from './event-dates-by-performances.pipe';

describe('EventDatesByPerformancesPipe', () => {
  it('create an instance', () => {
    const pipe = new EventDatesByPerformancesPipe();
    expect(pipe).toBeTruthy();
  });
});
