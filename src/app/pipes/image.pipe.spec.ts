/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ImagePipe } from './image.pipe';

describe('Pipe: Image', () => {
  it('create an instance', () => {
    let pipe = new ImagePipe(null);
    expect(pipe).toBeTruthy();
  });
});
