/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { VenueObjectMatcherPipe } from './venue-object-matcher.pipe';

describe('Pipe: VenueObjectMatcher', () => {
  it('create an instance', () => {
    let pipe = new VenueObjectMatcherPipe();
    expect(pipe).toBeTruthy();
  });
});
