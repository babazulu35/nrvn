/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { VenuesByPerformancesPipe } from './venues-by-performances.pipe';

describe('VenuesByPerformancesPipe', () => {
  it('create an instance', () => {
    const pipe = new VenuesByPerformancesPipe();
    expect(pipe).toBeTruthy();
  });
});
